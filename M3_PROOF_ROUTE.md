# Proof route and actual-factorization boundaries

1. Positive least critical relations follow from nonsymmetry: a pure relation
   would yield actual two-generator gluing and hence symmetry. Pair bounds
   and the positive weighted identity give the cyclic coefficient sums.
2. The n0-Apéry L-shape is proved in both directions. Its maximal elements
   give the exact two tail PF elements; these are genuinely distinct gaps.
3. An actual row's frozen support inclusion gives singleton or missing-direction
   support. A maximal gap on that ray gives the six arm formulas and bounds.
4. Actual coefficient replacement in the same complement gives the critical
   box. Pure signed kernel vectors inside the difference box vanish; floor
   reduction then proves the saturated integer kernel basis, not merely a
   rational span.
5. Singleton antichains cap every complement and every W factorization; a cap
   is attained. Three singletons confine the whole W fiber before uniqueness
   is invoked, giving full-Q cardinality three. BOX-W saturation uses the
   exact integer kernel and the attained cap, not global fiber uniqueness.
6. A matched pair first synchronizes depths. Actual A and B positive-m returns
   are minimized independently. The socle-gap argument shows the returns are
   outside H; the four caps apply to completed F+n0 vectors. Unequal levels
   produce completed nonnegative F vectors, even at zero gap boundaries.
   Equal levels cancel m before critical-box uniqueness is applied.
7. Same/mixed-color signed relations are used only for kernel calculations.
   Membership conclusions use completed nonnegative vectors. In particular,
   BA-W retains R=0 until an actual nonnegative completion is available.
8. Actual cyclic permutations and full color reversal transport H, Gamma,
   Setting, Q, Herzog socles, supports, and named actual coefficient vectors.
9. The finite four-label classifier is proved by ordinary Lean `decide`, whose
   proof is kernel checked. No native_decide or external enumerator supplies
   mathematical evidence. It does not assert |Q|=4.
10. Normalized terminal extraction derives all scalar identities from the
    actual rows. TYPE II's lambda ≤ b0 is proved by excluding the wrong
    same-color case. PATH proves positive R and both endpoint rays. CHAIN
    derives all positive gaps and four complement identities.

Appendix B's proved groundwork and the two remaining implications are
described separately in `M3_EXTERNAL_INPUT_REPORT.md`. No Section 5–10
terminal exclusion is used in this route.
