# Future modules

M1 ends at FOUNDATION + C2. Future classifications and certificate theorems have
no placeholder declarations or assumed external results in the current project.

Future critical multiplier interfaces should consume pure tail relations only,
and future packet replacement must retain the `replaceWithinActualFactorization`
containment requirement. The only current full-paper target is the unproved Prop
definition `P21MainStatement`.
