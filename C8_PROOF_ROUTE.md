# C8 proof route

The first successful lattice point supplies the packet and triangle data.
Boundary synchronization and multiplicity exclude `Croot = alpha`. In the
strict region, `chi ≤ T` creates only `OneData.RegionU`; `T < chi` creates only
`RegionD`. The two branches are disjoint and exhaustive. Region-D never
receives the window-only ONE, determinant-one, parameter, or level-split facts.
