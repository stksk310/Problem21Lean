# M3 independent statement and source review

Review date: 2026-09-17. Frozen baseline: `9a9e01c401a934cfca2da15026986b0ecf83ff4f`.
Reviewed snapshot: 32 modules under `P21/Nonsymmetric`, including `ActualLabels.lean` and all three exact extraction constructors. `ActualClassification.lean` was not yet present. Other agents were actively implementing the remaining bridge, so this report is a snapshot, not certification of later edits.

## Independence and scope

The reviewer did not author the Herzog, row geometry, relabeling, finite classification, or extraction modules. The reviewer **did author the ColorCap modules**; their examination here is a disclosed self-review, not an independent second review. A separate reviewer should inspect that subtree before claiming independently reviewed COLOR-CAP. No Lean sources were edited during this review. This is a logical-strength/source review, not a replacement for the final fresh build and generated axiom audit.

Compared theorem interfaces and critical proof steps with REQUEST sections 25–40, the extracted authoritative publication, and the readable/frozen Appendix B and section 4 excerpts. No source discrepancy requiring a mathematical repair was identified in the passages examined.

## Findings and release gates

1. **Full-success gate: COLOR-CAP is still conditional.** `ColorCap/Residuals.lean:12` defines, rather than proves, `MinimumOneStatement`; `ThreeArms.lean` likewise defines `BoxPositiveExitStatement`. `three_arms_impossible_of_residuals` at Residuals line 24 is a proved implication with both propositions as explicit arguments. The White companion inequalities and parameter contradictions do not establish relative empty-tetrahedron classification, and the proved path cases do not establish the whole positive-exit statement. Keep both residuals in any downstream theorem signature and mark unconditional COLOR-CAP and full M3 success false. Do not describe importing `Residuals` as discharging them. This is an acknowledged partial result, not an invalid Lean theorem.

2. **Full-success gate: finite labels are not yet the actual selected-four theorem.** `FourRowCombinatorics.lean:62` proves `Compatible t → Terminal t`. `Compatible` includes the same-color cap and all geometric exclusions. `ActualLabels.lean` constructs an injective four-label realization but does not prove compatibility. The snapshot therefore does not establish section 36 from four actual Q rows. The needed next bridge must derive every compatibility conjunct from the actual rows, with only the two explicit ColorCap residuals if a conditional milestone is intended. It must then construct one of the exact terminal input packages after genuine cyclic/reversal transport. A theorem taking `Compatible`, a terminal classification, or already normalized terminal rows as additional assumptions is not the requested full selected-four classification.

3. **Documentation correction:** `FourRowCombinatorics.lean:31` says every constraint has an actual-row geometric proof upstream. At this snapshot the same-color constraint has only the conditional reduction above and the actual compatibility bridge is unfinished. Qualify this comment and associated status reports accordingly. The finite theorem itself is valid at its stated conditional scope.

4. **Integration convention:** `RowLabel.A` is `arm false` and `RowLabel.B` is `arm true` (FourRowCombinatorics lines 19–20); `MinimumOneStatement` and `three_arms_impossible_of_residuals` use `true` for A and `false` for B. Any generic bridge must pass `!c`, or split the two colors explicitly. No incorrect existing call was found; the bridge was still being implemented. Add explicit coverage for both branches to prevent a silent semantic label mismatch in the stated interface.

## Boundaries checked without finding a defect

- `FourDistinctActualQRows` stores four actual full-Q memberships and injectivity. It does not replace the ambient Q by a four-element set. Selection from cardinality at least four and full-Q three-singleton collapse remain distinct claims.
- Support uses the frozen inclusion `D(complement) ⊆ SH(q)`. `missing_complement_zero` and `singleton_coeff_zero` consume that inclusion, without asserting equality.
- `ComplementGeometry` bounds every named actual complement factorization before deriving uniqueness by the critical box. `Saturation` constructs the nonnegative BOX-W vector and compares it with an attained singleton cap; it does not assume uniqueness of the entire W factorization fiber.
- Same/mixed-color relations are initially pure signed tail relations for kernel calculations. Membership is introduced through explicit nonnegative integer/Nat coefficients. Shared mixed coefficients may be zero. The derived positivity used in endpoint exclusions comes from the actual complement antichain.
- `MatchedPairData` keeps `gapJ` and `gapK` nonnegative, not strictly positive. `ReturnLevels` constructs the two positive minimum levels independently, compares them, and only invokes critical pure-tail rigidity after the m terms cancel. Its membership constructors require all four coefficients to be nonnegative. Zero-slack cases survive through matched-pair singleton exclusion.
- `PathInput`, `TypeIIInput`, and `ChainInput` refer to the same g, s, F, m, W and actual selected Q values. Their extraction constructors derive the complement identities. PATH includes the endpoint singleton complements, positive R, and all three ladder equations; TYPEII includes lambda ≤ b0; CHAIN now derives all four positive slacks. These are normalized extraction results, not by themselves existence for an arbitrary selection.
- PATH qA/qB are ladder-position names and their defining equations are explicit. They must not be reinterpreted as the A/B colors at the same endpoint.
- `Relabel` uses actual permutations, proves H and Gamma equality, preserves F/m/W/Q, and transports named coefficient vectors. Reversal exchanges a/b and fA/fB. This is an appropriate foundation for the pending terminal transport.
- Herzog positive critical data do not store the requested classification conclusion. The full data structure stores exact PF information, but `Herzog/PseudoFrobenius` actually constructs it from critical data, proving the two gaps, returns, exhaustiveness, and distinctness. The existence route uses the frozen gluing-to-symmetry direction to rule out pure critical relations; no M3 target theorem is used backwards.
- Tail cofiniteness is supplied explicitly to Herzog existence or obtained from the frozen canonical/nonempty-Q tail theorem in actual-row applications. No implication from nonsymmetry alone to cofiniteness was found. `TailPF` is actual tail gap plus all nonzero tail returns, not a substitute combinatorial definition.
- ColorCap actual minima are obtained from actual PF arms: a positive tail return puts the socle in Gamma, its proved Herzog gap rules out level zero, and Nat.find supplies the attained least level. Caps apply to that same actual vector. The conditional theorem does not assume the desired level-one witness as an input.

## Read-only dependency and integrity checks

A direct byte comparison against the frozen Git tree checked all 51 baseline Lean/config files (`*.lean`, lean-toolchain, lakefile.lean, lake-manifest.json): **zero changes**. The check did not claim to cover all frozen verification/report bytes; the main verifier must cover those separately.

The project import graph had **no cycles**. The 32-module subtree reached only these older project modules: Apery, Basic, CanonicalReduction, External.SymmetricThreeGenerator, Factorization, NumericalSemigroup, PseudoFrobenius, Selection, Semantics, Symmetric.Classification.CriticalRelations, Symmetric.Classification.GcdDecomposition, Symmetric.GlueNormalForm, Symmetric.StableCore, Symmetric.TwoGenerator, Tail. No later PATH/TYPEII/CHAIN exclusion, Euclidean-closure certificate, or main theorem was imported. A raw source scan found no sorry, axiom declaration, unsafe, opaque, native_decide, or run_tac in the reviewed subtree. Final lexical and kernel axiom audits remain the verification pipeline's responsibility.

## Accurate partial claim

Suggested current claim: “Proved nonsymmetric three-generator Herzog existence and exact tail PF description; actual row atlas, local complement geometry, singleton collapse and saturation, return-level rigidity and exclusions; normalized PATH/TYPEII/CHAIN extraction; genuine permutation transport; and finite compatible-label classification. Proved reduction of both actual color caps to the explicitly stated minimum-level-one and positive-exit obligations. The unconditional Appendix B obligations and the end-to-end actual selected-four terminal theorem remain incomplete at this snapshot.”

If the actual compatibility/terminal bridge is subsequently completed, update only that clause to a **conditional** selected-four terminal theorem, explicitly listing both ColorCap propositions. Do not promote this package to full M3 until both propositions have proofs and the final source/build/axiom audits pass.

## Subsequent integration addendum

After the review snapshot above, the reviewer was assigned implementation of `SelectedExtraction.lean`. That file is therefore **not independently reviewed by this report**. It compiled successfully with direct `lake env lean` and contains `actual_terminal_input_exists`, which consumes the actual labeling and terminal label conclusion, as well as the three conditional selected-four/cardinality wrappers. `TerminalInputExists` permits only cyclic relabeling or full reversal followed by cyclic relabeling. `SelectedTerminal` requires exact equality of the terminal package's row-value set with the selected rows, and `selectedValues_relabel` proves that relabeling preserves those values definitionally. No new residual proposition was added.

The separate kernel worker completed `ActualClassification.lean`, deriving every compatibility constraint with the two explicit ColorCap residuals. Thus finding 2's integration gap is now closed **conditionally**, pending the parent fresh build and complete axiom audit. The unconditional release gate in finding 1 remains open. The two color conventions were handled explicitly in the actual compatibility implementation. The accurate updated milestone is a complete actual selected-four terminal reduction conditional on precisely `MinimumOneStatement` and `BoxPositiveExitStatement`, together with the unconditional local results described above. This is still not full M3.
