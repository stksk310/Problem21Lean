# M3 dependency direction

```text
immutable M1 actual semigroup / C2 / selection interfaces
  + immutable critical-relation and two-generator gluing lemmas
        |
        +--> positive Herzog cycle --> exact PF pair --> six-arm atlas
        |
        +--> critical box --> saturated integer kernel --> primitive minors
                         |                         |
actual support inclusion + complement antichain    |
        |                |                         |
        +--> singleton caps/collapse/saturation    |
        +--> critical complement boxes ------------+
                         |
          matched / same-color / mixed-color geometry
                         |
          independent return minima + four caps
                         |
          equal-level rigidity + coexistence exclusions
                         |
       actual relabeling + finite four-label case split
                         |
   MINBOX [OPEN] + DPE [OPEN] --> actual color-cap implication
                         |
          actual selected-four conditional classification
                         |
          exact PATH / TYPE II / CHAIN input extraction
```

`Relabel` transports the actual semigroup and coefficient vectors; it does not
change m, F, W, or the selected integer values. `SelectedExtraction` is the
terminal composition root. `PrimitiveGenerators` supplies the separate
cofinite normalization of the critical minors needed for Appendix B.

The machine-readable import graph is included in local and CI evidence as
`DEPENDENCY_DAG_CHECK.txt`. No new module imports a Section 5 PATH exclusion,
Section 6 TYPE II exclusion, Sections 7–10 CHAIN closure, Euclidean closure,
certificate result, or a final Problem 21 theorem. No terminal configuration
is excluded by this candidate.
