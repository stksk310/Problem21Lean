# C7 dependency DAG

`Setup → PFiber → Extremal → Root → ColorReversal → Core → Returns → Caps → Slopes → Intrinsic → Orientation → Compact → ReturnLevels → ShiftNew → Chain`

Every arrow is a forward Section 7 dependency.  No C8–C10 module, CHAIN
impossibility theorem, PATH exclusion, or final Problem 21 theorem is imported.
