# Candidate self-check, not an independent audit ruling

The final target is an inhabitant of the exact unchanged
`ColorCap.MinimumOneStatement`, not a strengthened or renamed surrogate.
`StatementRegression.lean` requires that exact type and the DPE-only wrapper.
The scalar White proposition is itself inhabited by an internal Lean proof.

New proof code preserves the natural/integer coefficient distinction, actual
minimum, original element, tail-primitivity derivation and cyclic row order.
No bounded computational search supplies a mathematical proof. Ordinary finite
case distinctions are kernel checked; native_decide and run_tac are forbidden.

Internal reviews, with authorship and coverage disclosed:

- `verification/m3b1/GEOMETRY_REVIEW.md`
- `verification/m3b1/INTEGRATION_REVIEW.md`
- `verification/m3b1/VERIFICATION_REVIEW.md`

The verifier requires root, all old and all new modules to build; exact type
regressions; the unchanged lexical scanner; all new kernel-name axiom reports;
233-file Git/byte integrity; and four unchanged historical suites in separate
unbuilt project copies. Its namespace enumeration is read-only and constructs
no declaration or proof. The current new sources all use the audited namespace;
the scanner is not claimed to be a universal Lean parser.

The final local snapshot binds executable-source SHA256 values. GitHub CI adds
fresh Linux execution and exact candidate/checkout/commit equality. The separate
handoff receipt is written only after downloaded artifact digest, internal
manifest, successful job steps and exact head/run/attempt are verified.

Remaining mathematics is DPE only at the M3 residual boundary. Unconditional
COLOR-CAP and FULL G4 remain open. No main merge is performed.
