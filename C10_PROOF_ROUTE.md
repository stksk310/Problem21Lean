# C10 proof route

1. Project the frozen C9 `EuclideanSeed` to a canonical-free `EuclideanState`.
2. Derive the common HCR scale, matrix order, and elimination identities.
3. Split the exact Euclidean state into terminal and nonterminal cases.
4. In the terminal case, validate the 3234-positive-monomial certificate, convert the signed relation to nonnegative packets, and perform the replacement inside the same actual factorization of `W`.
5. In the nonterminal case, construct the next genuine packet, exchange colors, and prove strict decrease of `(E + chi).toNat`.
6. Close `EuclideanState.f_mem` by strong induction and contradict the frozen Frobenius gap.
7. Transport the contradiction through the frozen chain and Type-II wrappers to the public nonsymmetric `Q(F) ≥ 4` theorem.

