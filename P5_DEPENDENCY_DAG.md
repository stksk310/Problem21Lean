# P5 dependency DAG

```text
frozen PathInput / Herzog / C2
  -> Setup -> Returns -> Dual -> Central -> Packets -> Pair
  -> Normalization -> Comparison -> Seam -> PFree -> Split
  -> WeakRoot -> RootWalls -> RootNormalization -> EndpointLevel
  -> PairExclusion -> PFreeCoordinates -> PFreeSign
  -> PFreeAbsorption -> PFreeFix -> PFreeExclusion -> Exclusion
  -> Integration (TYPE II or CHAIN wrappers)
```

`Exclusion` has no dependency on `Integration`, Section 6, later CHAIN work, or
any final theorem.

